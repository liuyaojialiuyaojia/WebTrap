# Supplied input provenance

The paper and rebuttal-plan attachments were supplied on the old server. Their
old absolute attachment paths are not portable and the attachment blobs are
not duplicated in this repository.

`input_hashes.sha256` preserves the original filenames and SHA-256 values so a
restored copy can be verified without relying on the old server path.
